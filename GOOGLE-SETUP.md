# 讓存檔上雲端（Google 試算表，免費）

做完這 6 步，全部人的課表就會存到你的一份 Google 試算表裡，
換手機、換電腦都叫得出來。大約 5 分鐘。

---

### 1. 開一份新的試算表

網址列直接打 **sheets.new** 按 Enter。
把它取個名字，例如「修課盤存檔」。

### 2. 打開 Apps Script

在那份試算表上方選 **擴充功能 → Apps Script**。

### 3. 貼上程式碼

編輯器中間會有一段預設的 `function myFunction() {}`，
**全選刪掉**，把本專案的 `Code.gs` 內容整份貼上去，按存檔（💾 或 ⌘S）。

### 4. 部署成網頁應用程式

右上角 **部署 → 新增部署作業**

- 左邊齒輪「選取類型」→ 選 **網頁應用程式**
- 說明：隨便打，例如 `v1`
- **執行身分：我（你的信箱）**
- **具有存取權的使用者：所有人** ← 這項一定要選「所有人」，不然網站讀不到
- 按 **部署**

### 5. 授權

第一次會跳出授權：**授權存取權 → 選你的 Google 帳號 →**
出現「Google 尚未驗證這個應用程式」時，點左下 **進階 → 前往「專案名稱」(不安全)** → **允許**。

> 這是你自己寫的腳本，這個警告是因為沒有送 Google 審核，不是有問題。

### 6. 複製網址，填進網站

部署完成後會給你一段**網頁應用程式網址**，長得像：

```
https://script.google.com/macros/s/AKfycb.....................lQ/exec
```

複製它，然後打開 GitHub 上的 `index.html`（點鉛筆圖示線上編輯），
用 `Ctrl/⌘ + F` 找 `const CLOUD_URL`，把網址貼進引號中間：

```js
const CLOUD_URL = "https://script.google.com/macros/s/AKfyc..../exec";
```

存檔（Commit changes），等一分鐘網站就會改用雲端存檔了。
右上角的狀態文字會從「自動存於本機」變成「雲端存檔」。

---

## 之後

- 所有人存的課表都會出現在那份試算表的 `plans` 分頁，你可以直接看、直接刪。
- 每筆可以設 4 位數字密碼，別人沒有密碼就不能覆蓋或刪掉你那一筆。
- 這個密碼是明文存在試算表裡的簡單防呆，**不是真的資安機制**，不要用你其他地方在用的密碼。
- 沒填 `CLOUD_URL` 的話網站一樣能用，只是存檔只留在各自的瀏覽器裡。

## 改版本 / 重新部署

改過 `Code.gs` 之後要 **部署 → 管理部署作業 → 鉛筆 → 版本選「新版本」→ 部署**，
網址不會變。如果選「新增部署作業」會拿到一個新網址，那就要再改一次 `index.html`。
